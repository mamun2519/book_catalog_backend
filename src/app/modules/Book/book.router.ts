import express from 'express'
const router = express.Router()

router.get('/:id', () => {})
router.patch('/:id', () => {})
router.delete('/:id', () => {})
router.get('/:id', () => {})
router.post('/:id', () => {})

export const BookRoute = router
